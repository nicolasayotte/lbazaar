# Status: Milestone 4 — Platform Hygiene

**Spec:** `specs/m4.hygiene.md`
**Epic:** E-03
**Assessed:** 2026-02-27
**Method:** Automated codebase analysis against spec scenarios

---

## Feature Status Summary

| Feature | Priority | Status | Completeness | Blocking Issues |
|---------|----------|--------|--------------|-----------------|
| F-11: Remove Legacy Points UI | MUST | Partial | ~60% | Route redirects unverified; background process audit not done |

---

## F-11: Remove Legacy Points UI | MUST — Partial (~60%)

### F-11.1: Points UI fully removed — DONE

- Searched `resources/js/` for "point" and "points" references
- No points balance display, earning UI, or redemption UI found in React components
- The word "point" appears only in exam-related context (e.g., question points in `Exams/Create.jsx`, `Exams/Result.jsx`) — unrelated to the legacy points system

### F-11.2: Points-related navigation removed — DONE

- No sidebar, menu, breadcrumb, or link referencing points, point history, or point features found in navigation components
- No route labels reference "points"

### F-11.3: Points-related backend routes inaccessible — NOT VERIFIED

- Old points-related URLs may still be routable
- No redirect rules confirmed for previously-bookmarkable points pages
- Route files (`routes/web.php`, `routes/api.php`) not audited for leftover points routes

**Action needed:** Audit `routes/web.php` and `routes/api.php` for any remaining points-related route definitions. If found, either remove them or redirect to dashboard.

### F-11.4: Points references in shared components — DONE

- No points-related content, badges, counters, or labels found in shared components (user profiles, dashboards, activity feeds)
- Wallet history table shows transaction types but does not surface points data to UI

### F-11.5: Background processes investigated — NOT DONE

- **No investigation documented** as completed
- Known backend points presence:
  - `WalletTransactionHistory` model still has `points_before` and `points_after` as fillable fields
  - `WalletService` still reads/writes points data in `updateWalletTransaction()`
  - `CourseHistorySeeder` seeds points data

**Note (spec A-02):** Backend cleanup is explicitly out of scope — "Database tables, models, services, controllers, and API response fields are left in place for future cleanup." However, the **investigation** in F-11.5 is in-scope: the spec requires identifying any automated emails, notifications, or scheduled jobs that reference points, and disabling them if found.

**Action needed:** Investigate the following for points references:
1. Scheduled commands (`app/Console/Kernel.php`, `app/Console/Commands/`)
2. Email templates (`resources/views/emails/`)
3. Notification classes (`app/Notifications/`)
4. Queue jobs (`app/Jobs/`)
5. Event listeners (`app/Listeners/`)

If any active background process references points, disable or remove it. If none found, document the finding.

---

## Backend Points Footprint (Out of Scope per A-02, documented for reference)

The following backend code still references the points system. Per spec assumption A-02, this is **intentionally left in place** for future cleanup:

| Location | Description |
|----------|-------------|
| `WalletTransactionHistory` model | `points_before`, `points_after` fillable fields |
| `WalletService::updateWalletTransaction()` | Reads/writes points during wallet transactions |
| `CourseHistorySeeder` | Seeds points data |
| Migration `2023_02_22_000601` | `points_before`, `points_after` columns in schema |
| API response payloads | May include points fields in wallet transaction responses |

These do not violate the spec (which scopes F-11 to front-end only) but represent technical debt.

---

## Remaining Work

1. **Route audit (F-11.3):** Check `routes/web.php` and `routes/api.php` for legacy points routes. Add redirects to dashboard if found.
2. **Background process investigation (F-11.5):** Audit scheduled commands, emails, notifications, jobs, and listeners for points references. Document findings.
3. **Sign-off documentation:** Record that F-11.5 investigation was performed and its outcome (found/not found).
